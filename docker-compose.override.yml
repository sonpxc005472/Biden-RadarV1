services:  
  
  radar-okx:
   restart: unless-stopped
   environment:
     - ASPNETCORE_ENVIRONMENT=Development
     
  radar-bybit:
   restart: unless-stopped
   environment:
     - ASPNETCORE_ENVIRONMENT=Development
     
  # radar-gate:
   # restart: unless-stopped
   # environment:
     # - ASPNETCORE_ENVIRONMENT=Development
  
  # radar-binance:
   # restart: unless-stopped
   # environment:
     # - ASPNETCORE_ENVIRONMENT=Development
