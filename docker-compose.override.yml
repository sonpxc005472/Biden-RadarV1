version: '3.7'

services:  
  
  radar-okx:
   restart: unless-stopped
   environment:
     - ASPNETCORE_ENVIRONMENT=Development
     
  radar-bybit:
   restart: unless-stopped
   environment:
     - ASPNETCORE_ENVIRONMENT=Development
     
  radar-gate:
   restart: unless-stopped
   environment:
     - ASPNETCORE_ENVIRONMENT=Development
